package sample;

import javafx.scene.paint.Color;

public class Cueball extends Ball {
    public Cueball(double centerX, double centerY, double radius, Color colour) {
        super(centerX, centerY, radius, colour);
    }
}
